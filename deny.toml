[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "Zlib",
    "Unicode-3.0",
    "CDLA-Permissive-2.0",
    "ISC",
    "BSD-3-Clause",
    "MPL-2.0",
]

[unmaintained]
# Fail CI for unmaintained crates except the ones listed below
deny = true

[unmaintained.allow]
# Allow this specific unmaintained advisory (transitive via scraper)
"RUSTSEC-2025-0057" = "fxhash used transitively, cannot fix yet"
