name: Rust

permissions:
  contents: read
  pull-requests: write

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Install Rust using the supported action
      - uses: dtolnay/rust-toolchain@stable
        with:
          profile: minimal

      # Proper Rust caching
      - uses: Swatinem/rust-cache@v2

      # Run cargo audit (with correct permissions)
      - name: Run cargo audit
        uses: rustsec/audit-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run cargo deny
        uses: EmbarkStudios/cargo-deny-action@v1

      - name: Install tarpaulin
        run: |
          curl -sSfLo tarpaulin.tar.gz \
            https://github.com/xd009642/tarpaulin/releases/latest/download/cargo-tarpaulin-x86_64-unknown-linux-gnu.tar.gz
          tar xzf tarpaulin.tar.gz -C ~/.cargo/bin

      - name: Build
        run: cargo build --release
